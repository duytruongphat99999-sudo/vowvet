# allowed-tools.txt — VowVet. run-task.sh (headless) nạp file này.
# NGUYÊN TẮC: không bao giờ ghi "Bash" trần — scope theo từng lệnh.
Bash(bun install)
Bash(bun install --cwd:*)
Bash(bun run typecheck:*)
Bash(bun run build:*)
Bash(bun run --cwd api typecheck)
Bash(bun run --cwd web build)
Bash(python .claude/scripts/plan.py:*)
Bash(node --check:*)
Bash(bash .claude/scripts/verify.sh)
Bash(docker restart vowvet-api)
Bash(docker compose -f docker/docker-compose.yml up -d --build vowvet-web)
Bash(docker ps:*)
Bash(docker logs:*)
# curl chỉ localhost, có/không cờ -s (KHÔNG dùng Bash(curl:*) — quá rộng, curl -d @.env sẽ lọt)
Bash(curl -s http://localhost:*)
Bash(curl http://localhost:*)
# Bàn giao: agent ĐƯỢC push. Wildcard :* chỉ khớp đối-số-sau-dấu-cách nên
# "auto/:*" KHÔNG khớp "auto/health-uptime" (dính token) → dùng category rộng,
# guard.sh chặn cứng main/master/prod + --force (tuyến hai).
Bash(git push:*)
# Mở PR qua gh — credential trong keyring, agent không bao giờ thấy token
Bash(gh pr create:*)
