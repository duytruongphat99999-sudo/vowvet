# VowVet — Phân tích kiến trúc & quyết định (2026-06-08)

> Read-only deep-dive. Mọi luận điểm gắn `path:line` (đọc code thật). "CẦN VERIFY" = chưa đọc trực tiếp tới cùng. "brief lệch" = mô tả đề bài/handoff khác code thật.
> ⚠️ Workflow 6-agent dự định chạy fan-out **bị chặn bởi session-limit** (resets 2:30am) → báo cáo này do main-loop tự đọc/verify, không qua subagent.

---

## A. KIẾN TRÚC TỔNG

### A.0 — Đính chính brief (quan trọng)
- **brief lệch — Storage = Cloudflare R2, KHÔNG phải MinIO.** Code dùng `@aws-sdk/client-s3` (`package.json:7`) + R2 helper `api/src/lib/photos.ts:88` (`extractR2Key`), xoá object R2 khi replace ảnh (`photos.ts:114-163`). Không thấy MinIO trong repo.
- **docker-compose CHỈ có 2 service**: `vowvet-api` + `vowvet-web` (`docker/docker-compose.yml:12-65`). **Baserow & R2 là external** (Baserow qua `host.docker.internal:8888`, compose:26; R2 là cloud). Không có service `baserow`/`minio` ở compose này.

### A.1 — Cây mã
- **`web/src/pages/`** — ~90+ trang Astro SSR. Nhóm chính: `pets/[id].astro` (hồ sơ pet, file khổng lồ ~2580+ dòng, hub mọi tính năng) + `pets/[id]/*` (bcs, cognitive, pain, mobility, water, diary, routines, care-plan, vaccines, lost, memorial, triage…), `food-brands.astro` (catalog dinh dưỡng), `dashboard.astro`, `index.astro` (landing), `login/onboarding`, `playdate*`, `lost/*`, `memorial/*`, `vaccines`, `triage*`, `community/heroes/leaderboard`, `admin*`.
- **`web/src/components/`** — `TopBar.astro` (header app, CHỈ dashboard), `BottomNav.astro`, `EcosystemNav.astro`, `Logo.astro`, `FeatureIcon.astro`, `ui/{Button,Badge,Alert,Banner,Card,Toggle}.astro`, `dashboard/{PetHeroCard,QuickAccess,CarePlanProgress,QuestStrip,CommunityMini,PetScoreCompact,TopNudge,UrgencyBar}.astro`, `PhotoGallery`, `PersonalityRadar`, `PledgeCertificate`, `SectionActions`, `LangSwitch`.
- **`web/src/layouts/Layout.astro`** — shell duy nhất: meta/PWA/manifest, Alpine CDN (`Layout.astro:128-135`), SW register + update toast (`:138-232`), **KHÔNG render header** (chủ ý v207, `:6-7`), render `BottomNav` khi đăng nhập (`:83`), nav-skeleton chuyển trang (`:259-292`), dev-reset (`:85-95`, chỉ `import.meta.env.DEV`).
- **`api/src/`** — Hono trên Bun. `routes/` ~45 file (`public.ts`, `nutrition.ts`, `auth-*.ts`, `pets`*, `bcs`, `triage*`, `playdate`, `lost-pets`, `bills`, `voice-diary`, `personality`, `vaccines`, `memorials`, `heroes`, `community`…). `lib/` ~60 file (logic + Baserow access). `middleware/{auth,require-vet}.ts`. `scheduler.ts` + `lib/scheduler-jobs.ts` (cron: vaccine/care-plan/routine reminders, birthday, playdate-expiry, memorial-anniversary). `lib/me-cache.ts` (cache per-user TTL 20s).
- **`shared/`** — `nutrition-engine.ts` (DER/RER — **VÙNG KHÓA, canonical**), `health-conditions.ts` (**KHÓA**), `allergen-normalizer.ts`, `baserow.ts` (REST client), `baserow-config.ts` (table IDs), `care-plan-types.ts`, `breeds/age` helpers (ở `web/src/lib`).
- **`scripts/`** — ~90 file: migrations `migrate-m4…m31` + `migrate-*` lẻ; `e2e-*` (kiểm thử thủ công); `seed-*`; setup (`setup-baserow`, `generate-vapid`, `recover-baserow-config`); **bộ công cụ dinh dưỡng**: `premium-brands-populate.ts`, `oldbrand-nutri-backfill.ts`, `brand-shoppable-sync.ts`, `brand-search-fill.ts`, `brand-sync-*`. Tất cả chạy HOST, `BASEROW_URL=http://localhost:8888 bun run …`, dry-run mặc định.

### A.2 — Luồng data dinh dưỡng (Baserow → Alpine)
1. **Baserow** `food_brands` (table 648, 17 row) — schema verify read-only: `id, brand_name, product_line, species, life_stage, protein_pct, fat_pct, fiber_pct, carb_pct_calculated(number), calories_per_100g, price_vnd_per_kg, contains_allergens, mon_min_recommended, vn_availability, image_url, product_url`.
2. **Loader** `api/src/lib/nutrition.ts`: `loadFoodBrands()` (`:131-137`) → `listRows("food_brands", {size:200})`, **lọc bỏ row brand_name rỗng** (`:137` `.filter(r => r.brand_name && r.brand_name.trim())` → đây là lý do 2 junk row id1/2 vô hình trên catalog), `flatBrand()` (`:110-128`) map field + **giữ nguyên null** (`carb_pct_calculated`: `:120`). Cache in-memory **TTL 24h** (`:97 BRANDS_CACHE_TTL_MS`).
3. **HTTP** `api/src/routes/public.ts:124-169` `GET /api/v1/public/food-brands` (no-auth, filter species/life_stage/mon_min, sort mon_min-first).
4. **SSR** `web/src/pages/food-brands.astro:21` fetch `API_INTERNAL/api/v1/public/food-brands?size=50` → nhúng JSON vào `x-data="brandsCatalog(...)"` (`:124`).
5. **Alpine** `brandsCatalog()` (`:1307+`): `carbOf()` (`:2105`, ưu tiên `carb_pct_calculated` thật, fallback `estimatedCarb=100-P-F-12` `:2102`), `compatScore/isMismatch/mismatchReason/conditionDelta` (cảnh báo theo bệnh + carb mèo>25), `isRxDiet()` (miễn cờ carb đồ kê đơn). **DER/khẩu phần gọi engine** `shared/nutrition-engine.ts` (KHÔNG tính song song — đúng quy ước §4).

### A.3 — Docker & restart
- `vowvet-api` (`compose:13`): host `127.0.0.1:3010→3000`, `env_file ../.env` (`:22`), `BASEROW_URL=http://host.docker.internal:8888` (`:26`), mount `api/src`,`shared`,`baserow-config.json:ro`,`data/api` (gemini-usage log, `:37`). → **`docker restart vowvet-api`** khi sửa api/lib/data (bust cache 24h). `.env` đổi → `--force-recreate`.
- `vowvet-web` (`compose:43`): host `127.0.0.1:4322→4321`, `API_INTERNAL_URL=http://vowvet-api:3000` (`:55`), mount `web/src`,`web/public`,`shared`. → **`docker restart vowvet-web`** khi sửa `.astro` (mount Win→Linux không hot-reload). File tĩnh (`sw.js`) đọc disk mỗi request → không cần restart.
- **Baserow** = container ngoài (`localhost:8888`), không nằm compose này. **R2** = cloud. Cả 2 web/api join `bos-network` external (`:67`) cho Nginx Proxy Manager + Cloudflare Tunnel.

### A.4 — Secret/.env (CHỈ vị trí, KHÔNG giá trị)
- `./.env` (root) — nguồn env cho cả 2 container (`compose:22,52`). Gitignored (`.gitignore:14 .env*`).
- `./baserow-config.json` — field/table IDs, gitignored (`.gitignore:8`), mount ro vào api.
- `GEMINI_API_KEY` đọc từ env (`api/src/lib/gemini.ts:17`). `VAPID`/web-push, OAuth Google, OpenWeather, R2 keys → đều trong `.env`.
- ⚠️ **File chứa creds sống trên disk (gitignored, KHÔNG trong repo nhưng còn trên máy)**: `docs/archive/MIGRATION_REPORT.md` (token+password+email Baserow — `.gitignore:21-22` ghi rõ), `.env.backup` & `docker-compose.yml.backup` (`*.backup`, `.gitignore:16`). → xem D-bảo mật.

---

## B. 2 DESIGN-CALL

### B1. PALETTE TOKEN — emerald/sky vs "cấm màu"

**Mâu thuẫn (xác nhận):** `C:\docker\vowvet\CLAUDE.md §9` "Cấm màu: purple / navy / emerald" — NHƯNG `web/src/styles/global.css:20-21` định nghĩa `--color-vv-info:#2563eb` (blue) + `--color-vv-success:#10b981` (emerald) là **token chủ đích**, comment `:7` "VowVet sub-brand adds blue (info/health) + emerald (success)". → CLAUDE.md (brand) và global.css (kỹ thuật) **nói ngược nhau**.

**Blast-radius THẬT (grep web/src):**
- `emerald` = **282 hit / 59 file**; `sky-` = **144 hit / 22 file**; gộp class màu thật `(bg|text|border|ring|…)-(emerald|sky)-\d+` = **377 hit / 60 file**.
- **Shared component dính (sửa = lan mọi trang dùng):** `components/ui/Badge.astro`(3), `ui/Alert.astro`(4), `dashboard/QuestStrip.astro`(13), `dashboard/CommunityMini.astro`(8), `dashboard/CarePlanProgress.astro`(1). Đây là rủi ro lớn nhất — không phải "1 trang".
- **Nặng nhất (page):** `care-plan.astro`(~39 emerald), `pets/[id].astro`(19 emerald + 48 sky), `vet/dashboard`(~17), `lost/[slug]`(13), `vaccines`(8–9), `triage*`, `chat/*`, `playdate/*`.

**Hệ override `.pet-detail-tabs`** (`global.css` v62→v67, ~`:968-1395+`, kéo tới ~2581): **213 `!important` toàn file**. Ép sky→ink (`:1199`), emerald→gold `#B48608` (`:1213-1219`), slate→ink (`:1176-1196`), border-* → ink-12 (`:1234-1244`), "STRICT 3-COLOR" v64 (`:1168`), "NUCLEAR MONOCHROME" v67 (`:1289`). **Chỉ trong scope `.pet-detail-tabs`** → các trang khác KHÔNG được ép.
- **Rò rỉ (override không bắt):** gradient `from-sky-50/to-emerald-50` (`pets/[id].astro:1600`) + shade `-50` (override chỉ liệt `bg-sky-100`, không `-50`). → pet-tab vẫn lọt vài mảng sky/emerald nhạt.

**Override nên GIỮ hay DỌN?** Giữ ngắn hạn (nó đang là "tường lửa" giữ pet-detail đúng brand mà chưa phải refactor 19+48 class). Nhưng nó là **nợ**: 213 `!important` khó debug, dễ chồng, và chỉ vá 1 trang. Lý tưởng: dọn khi đã migrate class gốc.

**Phương án:**
| | Cách làm | Chi phí | Rủi ro | 
|--|--|--|--|
| **(a) Bỏ token & migrate toàn cục** | Xoá `--color-vv-success/info`, thay 377 class → ink/gold/amber-semantic ở 60 file | **Cao** (60 file, gồm shared comp) | Cao — đụng shared = regress nhiều trang; cần QA rộng |
| **(b) Scope lại rule** | Giữ token cho phần còn lại; chỉ chuẩn hoá pet-detail + tài liệu hoá "emerald=success token hợp lệ ngoài pet-detail" | **Thấp** | Thấp — nhưng KHÔNG giải mâu thuẫn §9 (phải sửa CLAUDE.md cho khớp thực tế) |
| **(c) Giữ nguyên + vá rò rỉ** | Chỉ thêm rule bắt gradient/`-50` trong `.pet-detail-tabs` | **Rất thấp** (vài dòng CSS) | Thấp — nhưng đắp thêm `!important`, nợ tăng |

**Khuyến nghị:** **(b) + một phần (c)** trước, hoãn (a). Lý do: (a) đụng shared component = blast-radius thật quá lớn cho 1 sprint, dễ vỡ; nên **sửa CLAUDE.md §9 cho khớp code** (emerald/sky là token semantic info/success HỢP LỆ ngoài pet-detail; pet-detail = monochrome) — biến "mâu thuẫn" thành "quy ước có chủ đích". Đồng thời vá rò rỉ gradient/`-50` ở pet-detail (c) để pet-tab sạch hẳn. (a) chỉ làm khi có ngân sách QA toàn site + lý do thương hiệu rõ. *Quyết định là của bạn — đây là khuyến nghị kèm lý do.*

### B2. APPHEADER

**Hiện trạng (verify):** KHÔNG có component AppHeader. `Layout.astro:6-7` chủ ý không render header (v207 "mỗi page header ngữ cảnh"). `TopBar.astro` là header component-hoá DUY NHẤT, brand chuẩn ink/gold/cream (`TopBar.astro:24` `border-mmp-cream`, badge gold, dot đỏ), **chỉ import ở `dashboard.astro:20,105`**. `<header>` inline ở **63 file**.

**Map độ lệch header:**
| Trang | Element | Border | Ghi chú |
|--|--|--|--|
| dashboard | `<TopBar>` component | `border-mmp-cream` | chuẩn brand, component-hoá |
| food-brands `:126` | `<nav>` inline | `border-mmp-ink/12` ✓ | đã brand (đúng) |
| index `:83` | `<nav>` inline | `border-mmp-ink/12` ✓ | **vừa fix phiên trước** (was slate-200) |
| pets/[id] `:224` | back-link `<a>` | text `mmp-ink/60` ✓ | **vừa fix** (was slate-500); KHÔNG có app-header thật, chỉ "← Dashboard" + hero + section-nav |
| pets/[id] `:827` | `#pet-page-nav` | `border-mmp-ink/12` ✓ | **vừa fix** (section tab-nav) |
| 63 file khác | `<header>` inline | hỗn hợp | nhiều `border-slate-200` còn sót (D-#7) |

→ **handoff khớp**: TopBar là cái duy nhất chuẩn; phần lớn trang dựng header riêng, border lệch rải rác.

**Phương án:**
| | Cách | Chi phí migrate | Rủi ro |
|--|--|--|--|
| **(a) Dựng AppHeader opt-in** (tổng quát hoá TopBar: props `backHref/title/showNavIcons/pet`) + roll-out dần | Tạo 1 component + thay từng trang (opt-in, không ép) | Trung bình, **tăng dần** (mỗi trang vài dòng) | Thấp nếu opt-in (trang chưa migrate giữ nguyên); cần test sticky/z-index từng trang |
| **(b) Giữ v207** (header inline per-page) + chỉ chuẩn hoá token border | Rất thấp (grep-fix màu) | Rất thấp | Không giải "lệch cấu trúc", chỉ giải màu |

**Khuyến nghị:** **(a) opt-in, KHÔNG migrate ồ ạt.** Tổng quát hoá `TopBar.astro` thành `AppHeader.astro` (giữ TopBar làm preset dashboard), roll-out theo độ ưu tiên: **`pets/[id]` lệch nhất** (không có app-header thật) → rồi các trang gated chính (vaccines/alerts/settings). Giữ v207 cho trang public/landing (header marketing khác bản chất). Lý do: opt-in tránh big-bang regression, vẫn tiến tới nhất quán. *Quyết định của bạn.*

---

## C. ROADMAP FEASIBILITY

**Hạ tầng AI sẵn có (quyết định khả thi):** `api/src/lib/gemini.ts` — SDK `@google/genai` (`:11`), model `gemini-2.5-flash`/`pro` (`:27`), cost-tracking + budget $5/ngày (`:18-19`). **Gemini VISION đã chạy ở nhiều endpoint**: `bcs-vision.ts`, `lost-pet-vision.ts`, `bills.ts` (OCR hoá đơn), `voice-diary.ts`, `care-plan*`, `triage.ts`, `analytics.ts`, `birthday-events.ts` (grep 12 file). → ảnh→Gemini→JSON là **pattern đã có sẵn**.
- **deps hiện tại** (`package.json`): chỉ `@aws-sdk/client-s3`, `web-push`, `zod`. **KHÔNG có lib barcode, KHÔNG có lib PDF-parse.** (SL-3: thêm dep phải xin phép.)

### C.1 — Camera scan `/scan`
Stack OK: Astro+Alpine client `getUserMedia` → chụp frame → POST base64 → api → Gemini (như `bills.ts`). 3 hướng:
- **(ii) OCR nhãn → guaranteed-analysis → số** ⭐ **NÊN LÀM TRƯỚC.** Tái dùng 100% Gemini-vision sẵn có (no new dep, hợp SL-3). Gemini đọc ảnh nhãn → JSON `{protein, fat, fiber, moisture, ash}` → tính carb NFE `100-P-F-fibre-moisture-ash` → feed thẳng `carbOf`/so sánh. **Đúng trọng tâm sản phẩm** (so thức ăn số thật). Scope nhỏ nhất: 1 endpoint `/scan` chụp 1 nhãn → trả bảng GA + carb ước tính (CHƯA ghi Baserow). Rủi ro: độ chính xác OCR nhãn mờ/cong → cần cho user sửa số trước khi dùng.
- **(iii) Chụp THÚ → AI đánh giá** — **gần như ĐÃ TỒN TẠI** (`api/src/lib/bcs-vision.ts` chấm BCS từ ảnh). `/scan` cho pet chỉ là route mới gọi lại bcs-vision. Effort thấp nhất, nhưng không phải "scan sản phẩm".
- **(i) BARCODE → nhận diện SP** — **YẾU NHẤT, làm sau.** Cần lib quét client (`html5-qrcode`/`zxing-js` = **dep mới**, SL-3) + nguồn map barcode→SP: `food_brands` **KHÔNG có field barcode** (phải thêm cột → xin duyệt schema §4/§8) hoặc gọi Open Food Facts (độ phủ **hàng pet VN rất thấp** → tỉ lệ trúng kém). Data-gap là rào cản chính, không phải kỹ thuật.

**Khuyến nghị thứ tự: (ii) OCR nhãn → (iii) wrap bcs-vision → (i) barcode (chỉ khi có nguồn data).**

### C.2 — PDF pet record (extract)
- **Khả thi, KHÔNG cần dep mới**: `@google/genai` nhận **PDF input** (inlineData `application/pdf`) → 1 call trả JSON `{vaccine_name, date, next_due, weight, conditions...}`. Xử lý được cả PDF scan (ảnh) lẫn PDF text. *CẦN VERIFY: version `@google/genai` đang dùng có bật PDF input — kiểm `package.json`/`bun.lock` + thử 1 call.*
- Phương án dep mới (không khuyến nghị): `unpdf`/`pdf-parse` (chỉ PDF text, fail PDF scan) → SL-3 gate.
- **Độ khó: trung bình.** Scope nhỏ nhất: upload 1 PDF sổ tiêm → Gemini trích 3 field (tên vaccine/ngày/hạn kế) → hiện preview cho user xác nhận → (giai đoạn sau) ghi `vaccines` table. Phân biệt: **đây là EXTRACT**, khác nút "Xuất PDF Hộ chiếu" đã có (`pets/[id].astro:225`, đó là GENERATE qua `@media print` `global.css:748+`).

---

## D. NỢ KỸ THUẬT & RỦI RO (cao → thấp)

1. **🔴 BẢO MẬT — creds sống trên disk.** `docs/archive/MIGRATION_REPORT.md` chứa **token+password+email Baserow** (chính `.gitignore:21-22` ghi rõ), + `.env.backup`/`docker-compose.yml.backup` (`*.backup`). Đã gitignore (KHÔNG vào repo) nhưng còn trên máy → **khuyến nghị: rotate token Baserow + xoá file archive creds**. (Repo local-only giảm rủi ro, nhưng vẫn là nợ.)
2. **🟠 Carb convention DRIFT giữa 2 đợt số** (tính từ data verify). Residual ẩn `moisture+ash = 100-(P+F+fibre+carb)`:
   - **8 row CŨ (backfill):** 15.7–17.0 → **nhất quán ~16** (NFE convention chuẩn).
   - **9 PREMIUM (số Bồ đưa):** **6.5–22.0 — loạn**. Đáng ngờ: `id23 Hill's SD Indoor` residual **6.5** (P36+F15+fib8.5+carb34=93.5 → moisture+ash chỉ 6.5%, **phi lý** cho hạt khô ~8-10% ẩm), `id22 Farmina Dog` residual **9.0** → carb 2 dòng này nhiều khả năng **thổi cao**. So với `estimatedCarb=100-P-F-12` (`food-brands.astro:2102`, giả định residual=12, bỏ qua fibre tách riêng) → **3 convention khác nhau cùng tồn tại**. → Nên rà lại carb premium theo cùng công thức NFE (TASK riêng, duyệt số §4).
3. **🟠 Đống CSS `!important`**: **213 occ** trong `global.css` (chủ yếu `.pet-detail-tabs` v62-v67). Khó debug/maintain, chồng tầng; gắn chặt với B1.
4. **🟡 2 file CONTEXT_SYNC trùng & LỆCH.** `CONTEXT_SYNC.md` (root) = **STALE**: "2026-06-07 15:17 · HEAD 105fa1d · SW v293 · 130 commit" (`:1-4`), trong khi `docs/CONTEXT_SYNC.md` (canonical) đã ở v297/v298, HEAD 9ef3a71. → root gây nhầm cho phiên sau. **Nên xoá/gộp root** (đã thống nhất docs/ là canonical).
5. **🟡 Rác `.bak`/`.backup` trên disk (~15 file).** `web/src/pages/pets/[id]/pet-score.astro.phase{1..8}*-pre.bak` (~12 file!), `sw.js.v184.bak`, `p/_legacy-qr.astro.bak`, `Layout.astro.bak-before-swfix`, `docker-compose.yml.backup`. Đều gitignored (`*.bak*`/`*.backup`) → git sạch, nhưng **clutter disk + dễ đọc nhầm**. History đã ở git → xoá được.
6. **🟡 Seed rác dễ lọt.** Chống duy nhất là loader lọc `brand_name` rỗng (`nutrition.ts:137`) — nên 2 junk row id1/2 từng vô hình mà vẫn nằm DB (đã xoá phiên trước). Không có validation tầng ghi → script seed cẩu thả còn lọt. (Thấp: đã có RECON-guard ở script mới.)
7. **🟡 Border slate off-brand còn sót.** Sau phiên header-fix: `index.astro` còn **5** `border-slate-200` (ngoài header), `pets/[id].astro` còn nhiều `slate-*` trong markup (được `.pet-detail-tabs` override che). Rải ở 63 file header inline. Nợ nhất-quán-màu, không gấp.
8. **🟢 SW auto-reload risk.** `Layout.astro:171-177` `controllerchange → window.location.reload()` — lịch sử từng gây reload loop (memory note). Hiện có guard `_reloading` (`:172`). CẦN VERIFY còn loop ở route 302 không (đã ghi nhận `/personality` cũ).
9. **🟢 Cache stale window.** `brandsCache` TTL 24h (`nutrition.ts:97`) bust THỦ CÔNG bằng `docker restart vowvet-api` — quên restart sau khi sửa data = thấy số cũ tới 24h. me-cache 20s (ổn). Vận hành-debt, không phải bug.
10. **🟢 `pets/[id].astro` quá khổ (~2580 dòng).** Hub mọi tính năng + tabs inline → khó sửa, dễ va Alpine scope (memory note `lab not defined`). Nợ refactor dài hạn.

---

## E. QUICK WINS (làm ngay, rủi ro thấp)

1. Xoá rác `.bak`: `web/src/pages/pets/[id]/pet-score.astro.phase*-pre.bak` (~12 file) + `sw.js.v184.bak` + `Layout.astro.bak-before-swfix` + `p/_legacy-qr.astro.bak` (history ở git).
2. Xoá/gộp `CONTEXT_SYNC.md` (root, stale v293) → giữ `docs/CONTEXT_SYNC.md` canonical.
3. Xoá creds-on-disk + rotate: `docs/archive/MIGRATION_REPORT.md`, `.env.backup` (bảo mật, D-#1).
4. Ẩn cảnh báo carb / rà lại carb 2 dòng premium nghi thổi: `id23 Hill's SD Indoor` (carb 34, residual 6.5), `id22 Farmina Dog` (carb 30, residual 9.0) — TASK số §4.
5. Host ảnh pack-shot 9 premium → `image_url` đang trống (card placeholder); upload `monminpet.com/images/products/<slug>.png`.
6. Sửa `CLAUDE.md §9` cho khớp code: ghi rõ emerald/sky = token info/success hợp lệ NGOÀI pet-detail (giải mâu thuẫn B1) — hoặc ngược lại, tuỳ quyết định.
7. Vá rò rỉ gradient/`-50` ở `.pet-detail-tabs` (`global.css`) nếu giữ override (B1-c) — vài dòng.
8. Vá `border-slate-200` lẻ còn sót `index.astro` (5 chỗ ngoài header) về `border-mmp-ink/12` nếu muốn nhất quán (rủi ro thấp, không gấp).

---

*Hết. Read-only — không sửa code/Baserow/commit. File này là artifact MỚI duy nhất; bạn tự commit.*
